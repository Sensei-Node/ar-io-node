# Force redirects from HTTP to HTTPS
server {
    listen 80;
    listen [::]:80;
    server_name ar-io-testnet.senseinode.com *.ar-io-testnet.senseinode.com;

    # location / {
    #     return 301 https://$host$request_uri;
    # }
    location / {
        proxy_pass http://envoy:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_http_version 1.1;
    }
}

# Forward traffic to your node and provide SSL certificates
# server {
#     listen 443 ssl;
#     listen [::]:443 ssl;
#     server_name ar-io-testnet.senseinode.com *.ar-io-testnet.senseinode.com;

#     ssl_certificate /etc/letsencrypt/live/ar-io-testnet.senseinode.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/ar-io-testnet.senseinode.com/privkey.pem;

#     location / {
#         proxy_pass http://localhost:3000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_http_version 1.1;
#     }
# }
